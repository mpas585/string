# GEN strings — 改修まとめ（10件）

プロジェクトルートに、このzipの中身をそのまま上書きしてください。

## 1. 運指のリセット（個別／全部）
- **個別**：運指の編集パネル内、音名の右に「この音だけリセット」を追加。
  その音のリード音と運指をおすすめの状態へ戻します。
- **全部**：「👇 運指を変更できます」の見出し右に ⟲ ボタンを追加。
  押すと確認ダイアログを出してから全音をリセットします。
- 変更：`src/modes.js`（`resetSelectedFingering()` 追加）, `src/main.js`, `includes/views/app.php`, `index.html`, 言語4種

## 2. 採点ゲームに β
- 入口のカード（🎮 採点ゲーム）とドロワーのタブの両方に β バッジを追加。
- 変更：`includes/views/app.php`, `index.html`, `src/styles.css`（`.beta-badge`）

## 3. 共有モーダルを上下左右中央に
- `#mShare` はドックの吹き出し（`.dkmodal`）を流用していたため左下寄りに出ていました。
  `#mShare` だけ画面中央に固定するよう上書き。
- 変更：`src/styles.css`

## 4. シェアボタンをハートの上へ／公開・非公開を文言と色で表示
- 指板の左上、ハートの**上**に「非公開／公開中」ボタンを追加（ハートは1段下げ）。
- **公開中は青**（`.sharebtn.on`）になります。アイコンだけでなく文言も併記。
- 自分がアップロードした譜面を開いているときだけ表示されます。
- サーバ側：`score_list()` に `shares` テーブルの LEFT JOIN を追加し、
  各行が `shared` / `share_id` を持つようにしました（今まで公開状態を知る術がなかったため）。
- 公開／公開停止の直後に一覧を取り直し、ボタンの表示が即座に切り替わります。
- 変更：`includes/scores.php`, `src/uploads.js`, `src/shares.js`, `src/main.js`, `includes/views/app.php`, `src/styles.css`, 言語4種

## 5. 削除ボタンを指運ビューの右上へ
- 指板の右上に 🗑 を追加（旧シェアボタンの位置）。確認ダイアログは従来どおり。
- 「譜面を読み込む」一覧の行からは削除ボタンを**撤去**しました（移動のため）。
  ※シェアは「追加」というご指示だったため、一覧側にも残しています。
- 変更：`src/uploads.js`, `src/main.js`, `includes/views/app.php`, `src/styles.css`

## 6. 採点：「録音して採点する」が下すぎる件
- ボタン行を `position:sticky` でドロワー下辺に貼り付け。
  課題曲を選んだらスクロールせずそのまま押せます。
- 「メトロノームに合わせて…」の注記は、貼り付けた帯に隠れないよう**ボタンの上**へ移動。
- 変更：`includes/views/app.php`, `src/styles.css`（`.gstart-bar`）

## 7. 再生ボタンの光り方を統一
- 淵が脈打つ演出（`fabattn`）を廃止し、**▶がオレンジに光る**演出（`fabled`）に一本化。
- 対象は3箇所すべて：設定変更後の催促・「✓保存しました」直後・曲読み込み直後。
- 未使用になった `@keyframes fabattn` は削除。
- 変更：`src/styles.css`

## 8. 上部バーを曲名だけに／自分の曲は名前を編集可能
- 上部バーから運指情報（`#nowline`）を撤去し、曲名のみに。文字も大きくしました。
- 曲名は `<button>` 化。自分のアップロード曲のときだけ枠と ✎ が付いて押せるようになり、
  その場で名前変更モーダルが開きます。それ以外は枠なしの見出しのままです。
- 名前変更後は上部バーの表示も即座に更新されます。
- `renderNow()` に要素なしのガードを追加（単体版 `index.html` は従来のまま動作）。
- 変更：`includes/views/app.php`, `src/modes.js`, `src/uploads.js`, `src/main.js`, `src/styles.css`

## 9. 「譜面を読み込む」一覧の見栄えを「曲を選ぶ」に合わせた
- 行間 5→7px、角丸 9→11px、最低高さ 52px、左右padding 12px を `.songbtn` と統一。
- 曲名 13→14px、説明 10px等幅→11px `--muted` と、字も一覧側にそろえました。
- 削除ボタンが抜けたぶん、下段の操作は 名前・トラック・シェア の3つになり窮屈さも解消。
- 変更：`src/styles.css`, `src/uploads.js`

## 10. 画像の軽量化
- PNG 17枚を pngquant + optipng（`-o7 -strip all`）で再圧縮。
- **388,315 → 200,217 バイト（-188KB / 48.4%削減）**
- 寸法・透明度は維持。白背景・黒背景に合成した差分RMSは全て 2.3 以下で視覚的な劣化はありません。
  （アイコン系は最大 -62%、楽器イラストとロゴは -10% 程度）

---

## 検証
- PHP：全 `.php` を `php -l` → エラーなし
- JS：`src/*.js` 24ファイルを `node --check` → 全て構文OK
- 言語：ja / en / es / zh の**748キーすべてが4言語に揃っていること**を確認
- 改行コード：変更した全ファイルで元の CRLF を維持
- 画像：全17枚の再読込・寸法一致・視覚差分を確認

## 補足
- `index.html`（PHPを通さない単体版）には元々ハートも `#scoretitle` も無いため、
  項目4・5・8の追加UIは入っていません。項目1・2は反映済みです。
  単体版にも揃えたい場合はお申し付けください。
- `src/lang-ja.js` は自動生成物ですが、`tools/langjson.py` が現在の `ja.php` を
  パースできない状態（一部の文字列で停止）だったため、該当キーのみ手で追記しています。

---

# 【修正】トップページのボタンが押せなくなった件

## 原因
項目8で上部バーから `#nowline`（押さえる音の行）を削除しましたが、
この要素へ書き込む箇所が **7箇所** あり、ガードを入れたのは2箇所だけでした。

残った5箇所が `document.getElementById('nowline').textContent = …` を素で実行しており、
とくに `applyMode()` の**モード未選択（＝トップ画面）の分岐**が起動直後に必ず通るため、
`Cannot set properties of null` で例外 → **以降の初期化が全部止まり**、
`updateChrome()` なども実行されず、画面のボタンが反応しなくなっていました。

## 対処
`src/util.js` に要素が無ければ何もしない `setNowLine(text, asHtml)` を追加し、
**7箇所すべてをこの関数経由に統一**しました（今後この行を消しても壊れません）。

- `src/util.js` … `setNowLine()` を追加
- `src/modes.js` … 4箇所（`applyMode` の3分岐 + `renderNow`）
- `src/tuner.js` … 2箇所
- `src/fingerboard.js` … 1箇所

## あわせて修正：⚙ が ☰ の隣に寄っていた件
`.scoretitle:empty{display:none}` にしていたため、曲を開いていないときに
中央のスペーサーが消えて ⚙ が左端へ寄っていました。
空のときも場所は残し、枠と ✎ は `.editable` のときだけ出るよう変更しています。

## 検証（ヘッドレスChromeで実機同等に確認）
| 画面 | 結果 |
|---|---|
| トップ（モード選択） | 例外なし／3枚のカードすべてクリック可 |
| チューナー | 例外なし／⚙ が右端（x=334）に復帰 |
| 曲練習（譜面なし） | 例外なし／曲名表示OK |
| 曲を選択後 | 例外なし／曲名が上部バーに反映 |

JavaScript エラー：**0件**（修正前は起動直後に `PAGEERROR` 1件）

---

# 【追加修正】スクショ①〜③ ＋ langjson.py

## ① 曲名が出ていない
`openUpload()` が `setScore(parsed, curScore)` と**第3引数（表示名）を渡していません**でした。
`curScore` は `'up:12'` という運指保存用の内部IDで、表示名は別引数です。
`setScore(parsed, curScore, r.name)` に修正し、アップロードした譜面の曲名が上部バーに出るようにしました。

**あわせてもう1件、スクショから見つかった不具合を修正:**
何も開いていないのに「非公開」「🗑」が出て、曲名欄も編集可（✎付き）になっていました。
`curUploadItem()` が「`ST.scoreName === curScore` なら自分の譜面」と判定していたため、
譜面を閉じて両方 `''` になると一致してしまっていたのが原因です。空でないことを先に確認するようにしました。
（スクショ①はまさにこの状態で、曲名が空なのにボタンだけ出ていました）

## ② 一覧から「名前」「シェア」を削除
`.uprow` の下段ボタンは「トラック」だけになりました（元のMIDIを預かっている行のみ）。
名前の変更は上部バーの曲名から、シェアは指板の左上、削除は指板の右上と、操作を1箇所に集約しています。
`main.js` の `#upList` ハンドラからも `.ur` / `.us` / `.ud` の分岐を削除しました。

## ③ ゴミ箱アイコンを分かりやすく
絵文字 🗑 は小さく潰れて判別できないため、**線画のSVG**に差し替えました。
ボタンも 40→44px に拡大し、アイコンは 23px、色は赤系（#e2607a）にして役割が伝わるようにしています。

## ④ tools/langjson.py がパースできない件
原因はご指摘のとおり **`"..."` と `'...'` の混在**でした。
パーサがシングルクォートしか処理せず、`"本当に退会しますか？\n…"` の行で停止していました。

`string()` をダブルクォート対応にし、PHP のエスケープ（`\n` `\t` `\r` `\v` `\f` `\e` `\\` `\"` `\$`、
`\x41` 形式、`\u{1F600}` 形式）を解釈するようにしました。
意味を持たないエスケープはPHPと同じくバックスラッシュごと残します。

**検証：** ja / en / es / zh の4ファイルすべてで、
`langjson.py` の出力が **PHP 自身の `require` 結果と完全一致**することを確認済みです。
これを使って `src/lang-ja.js` を正規の手順で生成し直しました（`intro` は従来どおり除外）。

## 検証（ヘッドレスChrome）
| 状態 | 曲名 | 編集可 | 非公開ボタン | 🗑 |
|---|---|---|---|---|
| 譜面なし | （空） | – | 出ない | 出ない |
| 用意した曲 | Cメジャースケール | – | 出ない | 出ない |

JavaScript エラー：**0件**

---

# 【追加】一覧デザイン統一 ＋ 上部バーのマーキー

## ① 「譜面を読み込む」一覧を「曲を選ぶ」と同じデザインに（ハート付き）
前回はCSSの数値を似せただけでしたが、今回は**同じCSSを共有**する形にしました。
markup 側で `.uprow` に `.songbtn` を併せ持たせ、CSSは差分だけを足しています。

- `[トラック]` ボタンを中に入れる都合で `button` の入れ子にできないため要素は `div` のままです。
  地色・枠がボタン由来だったので、基底規則を `button, label.filebtn, .uprow` に広げました。
- ブラウザで実際に並べて描画し、**曲名の太字・角丸・余白・説明文の色/サイズ・選択中の強調**が
  一致するまで合わせ込みました（選択中は枠と文字色だけが変わる `.songbtn.on` と同じ挙動に統一）。
- `[トラック]` は横いっぱいに広げず小さなチップにして、行が重たく見えないようにしています。

**ハートについて：** アップロードした譜面は、そもそも**お気に入りに入れられませんでした**。
`favKeyOfScore()` が `up:` を対象外にしていたためです（`song:` と `share:` のみ対応）。
`'up:12'` をそのままキーとして扱うようにしたので、一覧のハート表示に加えて
**指板の左上のハートでの付け外しも効くようになりました**。

## ② 上部バーの曲名は、入りきらないときだけマーキー
`renderScoreTitle()` で描画後に1フレーム待ってから実寸を測り、
はみ出す幅が 4px を超えるときだけ `.marquee` を付けます。収まるときは従来どおり `…` で省略します。

- 動く量（`--mq-shift`）とかける時間（`--mq-time`）は、はみ出した量から計算します
  （速さは約18px/秒、最短6秒）。長い曲名ほどゆっくり動きます。
- 端で少し止まるよう、`0〜14%` と `86〜100%` を静止区間にした往復アニメーションです。
  同じ文言を2つ並べて流し続ける方式は、短い曲名で不自然になるため採用していません。
- ✎ は右端に固定し、流れてきた文字がその下をくぐるようフェードを敷いています
  （並びの最後に置くと、曲名と一緒に画面外へ押し出されてしまうため）。
- 端末側で「視差効果を減らす」がONのときは動かさず省略表示に戻します（`prefers-reduced-motion`）。

## 検証（ヘッドレスChrome）
| 曲名 | 判定 |
|---|---|
| 「白鳥」（28px幅） | マーキーしない ✓ |
| 「Zwei Stucke fur Violoncello - Two Pieces…」（519px幅／枠318px） | マーキーする（-209px / 11.6秒）✓ |

一覧のデザイン一致もスクリーンショットで確認済み。JavaScript エラー：**0件**

---

# 【修正】一覧の行にボタンのグラデが当たっていなかった件

## 原因
ボタンの地色は基底規則（`button, label.filebtn`）の `background:var(--panel2)` ではなく、
後方の **「ボタン：立体感」** ブロックで上書きされていました。

```css
button, label.filebtn{
  background:linear-gradient(180deg, #302719, #241d14);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.07), 0 2px 0 rgba(0,0,0,.35);
  font-weight:600;
}
```

`.uprow` は `div` なのでこのブロックに入らず、**グラデーション・立体の影・太字（600）を全部取りこぼして**
基底のべた塗りだけになっていました。

（前回この差に気づけなかったのは、`getComputedStyle().backgroundColor` で比べていたためです。
グラデーションは `background-image` 側に入るので、色の層は透明のままになり差が出ませんでした。）

## 対処
値を書き写さず、**このブロックのセレクタに `.uprow` を加えて同じ規則を共有**するようにしました。
前回手当てしていた `font-weight:700` は、本体が 600 なので削除しています。
`:active` の沈み込みも同じ規則に含めたうえで、中の［トラック］ボタンを押したときに
親ごと二重に沈まないよう `.uprow .ubtn:active{transform:none}` を足しました。

## 検証
`#songBtns` の中に実物の `.songbtn` と `.uprow` を並べて計算値を比較し、
**backgroundImage / boxShadow / fontWeight / borderRadius / borderColor / padding / minHeight / fontSize
すべてが完全一致**することを確認しました（差分ゼロ）。
